<h1> <%= link_to "Wikinimals", articles_path %></h1>
<h1> <%= link_to "New Article", new_article_path %></h1>

<ul>
  <% @articles.each do |article| %>
    <h2><%= link_to "#{article.title}", article_path(article.id)%></h2>
    <p><%= "#{article.content}" %></p>
  <% end %>
</ul>
